# Local environment (contains secrets)
.env
.env.local
.env.*.local
.env.production
.env.staging

# Dependencies & build
node_modules/
dist/
*.tsbuildinfo

# Supabase CLI temp (project refs, pooler URLs)
supabase/.temp/

# Credentials
*.pem
credentials.json
service-account*.json

# Google Indexing API drip-feed cursor (do not commit)
.indexing-state.json

# Generated SEO tooling output (committed stub stays; GSC run overwrites locally)
# src/data/striking-distance.json is tracked with [] — uncomment to ignore live data:
# src/data/striking-distance.json

# Editor
.DS_Store
.vercel
.env*
